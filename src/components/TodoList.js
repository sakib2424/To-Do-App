import React from "react";

const TodoList = (props) => {
  //   const renderedList = props.todos.map((item) => <li>{item}</li>);
  return (
    <div className="todo-container">
      {/* <ul className="todo-list">{renderedList}</ul>  */}
    </div>
  );
};

export default TodoList;
